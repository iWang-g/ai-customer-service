# 开发环境默认通过 Vite 的 /api 和 /ws 同源代理访问 8001。
# 仅在业务服务部署到其他地址时覆盖：
# VITE_API_BASE_URL=https://api.example.com/api/v1
# VITE_KB_BASE_URL=https://kb.example.com/api/v1
# VITE_WS_URL=wss://api.example.com

# Electron 主进程和开发态 RPA 可按需覆盖同一组地址：
# BUSINESS_API_URL=https://api.example.com/api/v1
# KNOWLEDGE_BASE_URL=https://kb.example.com/api/v1
# BUSINESS_WS_URL=wss://api.example.com
# RPA_PYTHON_PATH=python

# 正式安装包不读取这些变量，而是使用打包进 resources 的 runtime-config.json。
# 本地测试包默认使用 config/release.json（当前指向 127.0.0.1）。
# 本地测试包可执行：
# pnpm dist:local
# 远程测试包可执行：
# pnpm dist:remote
# 或直接传入任意配置文件：
# pnpm dist -- -RuntimeConfigFile config/remote.json
