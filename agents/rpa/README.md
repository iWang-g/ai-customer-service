# 本机 RPA 节点

该进程由 Electron 主进程启动，通过标准输入/输出上的 JSON Lines 协议通信。业务访问令牌只通过进程标准输入传入，不写入命令行或日志。

节点功能：

- 向业务 API 注册节点并获取节点令牌
- 定时心跳和离线重连
- 同步本地拼多多店铺与服务端平台账号
- 将采集事件先写入本机 SQLite，再批量上传
- 退出时将节点标记为离线

当前阶段尚未接入拼多多 DOM 采集，`enqueue_event` 通道为阶段 C 预留。
