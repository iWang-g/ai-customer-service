# 千牛限定转接探针

本探针只允许转接 `tb542952183` 到同店铺在线客服“有求必应羊羊:城堡”。
它使用千牛页面的 `QNAbilityCenter.ability.invoke({cmd:'transferContact'})`，不切店铺、不切会话、不操作输入框。

执行前必须重新读取在线状态，并核对目标 UID；任何回执或上下文异常都按失败处理，不自动重试。

当前文件包含页面调用器和固定参数校验；完成本地服务封装、安装审计后才允许真实执行。
